{
  "name": "fullstack-workspace",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "vercel-build": "npm run install:all && cd client && npm run build",
    "vercel-start": "cd server && npm start"
  }
}
