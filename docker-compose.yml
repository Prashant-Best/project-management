services:
  backend:
    build:
      context: ./server
    env_file:
      - ./server/.env
    ports:
      - "3000:3000"

  frontend:
    build:
      context: ./client
      args:
        VITE_API_BASE_URL: http://localhost:3000/api
    depends_on:
      - backend
    ports:
      - "8080:80"
